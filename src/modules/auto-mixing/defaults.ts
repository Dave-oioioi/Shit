export const autoMixingState = {
  enabled: false,
  status: "音频场景模块已就绪",
  lastActionAt: null as string | null,
};

export const autoMixingSettings = {
  mode: "presentation",
  autoStart: true,
  aggressiveBalancing: false,
};
