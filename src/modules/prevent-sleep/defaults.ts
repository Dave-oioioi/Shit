export const preventSleepState = {
  enabled: false,
  status: "常亮守护模块已就绪",
  lastActionAt: null as string | null,
};

export const preventSleepSettings = {
  keepDisplayOn: true,
  keepSystemAwake: true,
  triggerPreset: "focus-session",
};
