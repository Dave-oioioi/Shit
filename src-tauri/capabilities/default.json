{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main SHIT VAULT window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
